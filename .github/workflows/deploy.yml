name: Test

# The app is deployed by Cloudflare Pages (connected to this repo) and the API by
# `docker compose up -d --build` on the VPS; this workflow only runs the tests.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm test
