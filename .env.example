# Copy to .env on the VPS and fill in.

# The app's address (Cloudflare Pages custom domain). Only this origin may call the API.
APP_ORIGIN=https://app.example.com

# From Cloudflare Zero Trust → Networks → Tunnels → your tunnel → the token in the install command.
TUNNEL_TOKEN=
